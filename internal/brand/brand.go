// SPDX-License-Identifier: MIT
package brand

// Stable identity fields (DataDirName, DBFileName, EnvPrefix, etc.) are generated
// in brand_gen.go from build/brand.yml. Change them only with a migration plan;
// existing installs rely on ~/.renbrowser/ and REN_BROWSER_* env vars.
